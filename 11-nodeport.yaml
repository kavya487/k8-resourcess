kind: Service
apiVersion: v1
metadata:
  name: serviceyaml
spec:
  type: NodePort
  selector: 
    app: nginx
    duration: ten
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30007

---

kind: Pod
apiVersion: v1
metadata: 
  name: podnginx
  labels:
    app: nginx
    duration: ten
spec:
  - containers:
    name: nginx
    image: nginx