kind: Service
apiVersion: v1
metadata:
  name: loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: lb
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
---

kind: Pod
apiVersion: v1
metadata:
  name: pod-lb
  labels:
    app: lb
spec:
  containers:
  - name: nginx
    image: nginx
    