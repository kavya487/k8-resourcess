kind: Deployment
apiVersion: apps/v1
metadata:
  name: nginx
  labels:
    app: nginx
    web: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
      web: frontnend
  template:
    metadata:
      labels:
        app: nginx
        web: frontnend
    spec:
      containers:
      - name: nginx
        image: kavya478/deployment:1.0.0.0

---

kind: service
apiVersion: v1
metadata:
  name: service-nginx
spec:
  type: LoadBalancer
  selector:
    app: nginx
    web: frontnend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80

