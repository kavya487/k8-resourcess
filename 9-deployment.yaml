kind: Deployment
apiVersion: apps/v1
metadata:
  name: deploykavya
  labels:
    app: web
    tier: backend
spec:
  selector:
    matchLabels:
      app: web
      tier: backend
  template:
    metadata:
      labels:
        app: web
        tier: backend
    spec:
      containers:
      - name: nginx
      - image: nginx

---

kind: Service
apiVersion: v1
metadata:
  name: servicesas
spec:
  type: LoadBalancer
  selector:
    app: web
    tier: backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30001